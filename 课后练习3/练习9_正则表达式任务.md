# 练习9：用编程智能体完成正则表达式任务

## 任务
匹配 Markdown 无序列表项（以 `- `、`* ` 或 `+ ` 开头的行）

## 最终正则表达式
```regex
^[-*+]\s

## 测试结果
echo "- item1" | grep -E '^[-*+]\s'  #  匹配
echo "* item2" | grep -E '^[-*+]\s'  #  匹配
echo "+ item3" | grep -E '^[-*+]\s'  #  匹配
echo "  - item" | grep -E '^[-*+]\s' #  不匹配（有缩进）


